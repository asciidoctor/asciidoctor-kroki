name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g. 0.19.0)'
        required: true

jobs:
  prepare:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      version: ${{ steps.set-version.outputs.version }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.4'
          working-directory: ./ruby
      - name: Set version
        id: set-version
        run: |
          VERSION="${{ inputs.version }}"
          VERSION="${VERSION#v}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
      - name: Bump npm and Ruby versions
        run: |
          VERSION="${{ steps.set-version.outputs.version }}"
          npm version "$VERSION" --no-git-tag-version
          # RubyGems uses dots for prereleases (1.0.0.beta.1), whereas npm/semver
          # uses a hyphen (1.0.0-beta.1). Converting avoids the .pre. RubyGems
          # would otherwise insert when normalizing the hyphenated form.
          RUBY_VERSION="${VERSION//-/.}"
          sed -i "s/VERSION = '.*'/VERSION = '$RUBY_VERSION'/" ruby/lib/asciidoctor/extensions/asciidoctor_kroki/version.rb
      - name: Update Gemfile.lock
        run: bundle lock
        working-directory: ./ruby
      - name: Roll the changelog
        run: node scripts/changelog.js release "${{ steps.set-version.outputs.version }}"
      - name: Commit and tag
        run: |
          VERSION="${{ steps.set-version.outputs.version }}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add package.json ruby/lib/asciidoctor/extensions/asciidoctor_kroki/version.rb ruby/Gemfile.lock CHANGELOG.md
          git commit -m "chore: release v$VERSION"
          git tag "v$VERSION"
          git push origin HEAD:main
          git push origin "v$VERSION"

  build-js:
    needs: prepare
    uses: ./.github/workflows/build-js.yml
    with:
      ref: v${{ needs.prepare.outputs.version }}

  build-ruby:
    needs: prepare
    uses: ./.github/workflows/build-ruby.yml
    with:
      ref: v${{ needs.prepare.outputs.version }}

  publish-js:
    needs: [prepare, build-js]
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: v${{ needs.prepare.outputs.version }}
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          registry-url: 'https://registry.npmjs.org'
      - name: Install dependencies
        run: npm ci
      - name: Build
        run: npm run build
      - name: Package
        run: |
          mkdir dist
          cd build
          zip -r ../dist/asciidoctor-kroki.dist.zip .
          tar -zcvf ../dist/asciidoctor-kroki.dist.tar.gz .
      - name: Publish to npm
        run: |
          NPM_TAG=latest
          case "${{ needs.prepare.outputs.version }}" in
            *-*) NPM_TAG=testing ;;
          esac
          npm publish --provenance --access public --tag "$NPM_TAG"
      - name: Upload JS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: js-dist
          path: dist/

  publish-ruby:
    needs: [prepare, build-ruby]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: v${{ needs.prepare.outputs.version }}
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.4'
          bundler-cache: true
          working-directory: ./ruby
      - name: Configure credentials
        run: |
          mkdir -p $HOME/.gem
          touch $HOME/.gem/credentials
          chmod 0600 $HOME/.gem/credentials
          printf -- "---\n:rubygems_api_key: ${RUBYGEMS_API_KEY}" > $HOME/.gem/credentials
        env:
          RUBYGEMS_API_KEY: ${{ secrets.RUBYGEMS_API_KEY }}
      - name: Build and publish gem
        run: |
          bundle exec rake build
          gem push pkg/*.gem
        working-directory: ./ruby

  release:
    needs: [prepare, publish-js, publish-ruby]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: v${{ needs.prepare.outputs.version }}
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - name: Download JS artifacts
        uses: actions/download-artifact@v4
        with:
          name: js-dist
          path: dist/
      - name: Generate release notes from the changelog
        run: node scripts/changelog.js notes "${{ needs.prepare.outputs.version }}" > release-notes.md
      - name: Create GitHub release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "v${{ needs.prepare.outputs.version }}" \
            --title "v${{ needs.prepare.outputs.version }}" \
            --notes-file release-notes.md \
            dist/asciidoctor-kroki.dist.zip \
            dist/asciidoctor-kroki.dist.tar.gz